{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "alignment-index-v1",
  "title": "Alignment Index (v1)",
  "description": "Per-alignment index of lexemes with at least one visible (e/f) pair.",
  "type": "object",
  "required": ["schema", "alignmentId", "lexemesWithVisiblePair"],
  "properties": {
    "schema": { "const": "alignment-index-v1" },
    "alignmentId": { "type": "string", "const": "syn--sblgnt-macula" },
    "lexemesWithVisiblePair": {
      "type": "array",
      "items": { "type": "string" },
      "description": "lexemeKey values that have ≥1 visible pair"
    }
  }
}
